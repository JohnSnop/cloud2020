server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml  #指配置格式是yml
        group: DEV_GROUP
        namespace: 01f15ad3-7b01-43a2-bcec-3a53be5c2466
